ProjectEnvDoctor 项目开发文档（新手极简版）

一、项目概述
1.1 项目基本信息
- 项目名称：ProjectEnvDoctor（项目环境医生）
- 项目类型：IntelliJ IDEA 自定义插件
- 适配平台：IntelliJ IDEA 社区版 / 旗舰版 2025.3+
- 开发语言：Kotlin
- 构建工具：Gradle IntelliJ Plugin
- 项目难度：入门级（无需源码AST/PSI解析）
- 当前版本：V1.1

1.2 解决的核心问题
- Maven/Gradle 仓库镜像异常、无法拉取依赖
- 项目编码混乱，GBK/UTF-8 混合导致中文乱码
- 缺少 .gitignore 导致误提交 IDE 配置文件
- .idea 配置损坏、模块 iml 文件异常
- 构建配置文件语法错误、缺失节点

1.3 项目定位
本插件为新手向极简环境诊断工具，不做复杂代码解析，仅通过 IDE 内置 API + 配置文件读取，快速检测项目环境问题，并给出标准化修复方案。
主打：低难度、高实用、易扩展、适合 IDEA 插件入门实战。

二、产品功能设计
2.1 功能入口
IDEA 项目视图 → 右键项目根目录 → 一键诊断项目环境
点击后弹出诊断弹窗，表格分类展示：正常项 / 警告项 / 错误项，每条结果附带问题描述 + 明确修复方案，支持一键复制完整报告。

2.2 核心检测能力（无PSI、纯配置检测）
本项目不使用 PSI、不解析 Java 源码 AST，仅读取配置文件与 IDE 环境参数，极大降低开发难度。

✅ JDK 环境检测（已完成）
- 获取 IDE 当前项目绑定 SDK 版本
- 读取 pom.xml / build.gradle 声明的 Java 版本
- 校验 JDK 版本兼容性

✅ Maven 环境检测（已完成）
- 检测 pom.xml 文件是否存在、是否损坏
- 读取本地 settings.xml 仓库路径、镜像配置

✅ Gradle 环境检测（已完成）
- 识别 build.gradle / settings.gradle 是否存在
- 检测构建文件是否为空、是否声明仓库

✅ 文件编码检测（已完成）
- 读取 .idea/encodings.xml 全局编码配置
- 检测项目编码是否为 UTF-8

✅ Git 规范检测（已完成）
- 检测根目录是否存在 .gitignore
- 校验是否忽略 .idea、target、build、*.iml 等敏感文件

2.3 诊断输出规范
统一三色分级输出：
- 🔴 错误：会直接导致项目爆红、构建失败
- 🟡 警告：存在潜在风险、不规范配置
- 🟢 正常：配置合规、环境正常
每条结果强制附带：问题说明 + 可视化修复方案。

2.4 可扩展进阶功能（后续迭代）
- 自动生成标准 .gitignore 文件
- 一键生成 Maven 国内镜像 settings.xml
- 诊断报告导出为日志文件
- SpringBoot、Docker 配置合规检测

三、技术方案与开发思路
3.1 技术栈（极简轻量化）
- 核心框架：IntelliJ Platform Plugin SDK
- 开发语言：Kotlin
- 构建工具：Gradle Kotlin DSL
- 核心思想：只读配置、不解析代码

3.2 核心依赖 API（新手友好）
全部为 IDE 原生基础 API，无复杂源码解析接口
- AnAction：实现右键菜单入口
- Project：获取当前项目上下文、项目路径
- VirtualFile：遍历项目文件、读取配置文本内容
- ProjectRootManager：读取项目 SDK
- DialogWrapper / JBTable：弹窗与表格展示

3.3 整体架构设计
采用 入口Action + 多检测器 + 模型封装 + UI弹窗四层极简架构
1. 交互层：右键 Action 触发诊断任务
2. 检测层：多个独立 Checker 各司其职（JDK/Git/Maven/Gradle/编码）
3. 模型层：统一封装诊断结果、状态、修复方案
4. 展示层：弹窗汇总展示所有诊断信息，支持一键复制
优点：解耦清晰、新增检测功能无需改动旧代码、极易维护。

四、项目目录结构
ProjectEnvDoctor/
├── src/main/kotlin/com/doctor/
│   ├── action/
│   │   └── ScanEnvAction.kt              // 右键菜单入口、串联所有检测器
│   ├── checker/                          // 所有环境检测器
│   │   ├── BaseChecker.kt                // 检测器统一接口
│   │   ├── JdkChecker.kt                 // JDK/SDK版本检测
│   │   ├── MavenChecker.kt               // Maven项目检测
│   │   ├── GradleChecker.kt              // Gradle项目检测
│   │   ├── EncodingChecker.kt            // 文件编码检测
│   │   └── GitIgnoreChecker.kt           // .gitignore规范检测
│   ├── model/
│   │   └── DiagnoseResult.kt             // 诊断结果数据模型（枚举+单项+汇总）
│   └── ui/
│       └── ResultDialog.kt               // 诊断弹窗UI界面（表格+复制）
├── src/main/resources/META-INF/
│   ── plugin.xml                        // 插件注册、菜单配置
├── build.gradle.kts                      // 插件构建配置
├── gradle.properties                     // Gradle配置（含编码设置）
├── CHANGELOG.md                          // 版本变更日志
├── README.md                             // 开源使用文档
└── 项目开发文档.md                       // 项目设计与开发文档

五、开发环境与部署规范
5.1 开发环境标准
- 编译 JDK：JDK 17（适配 IDEA 2025+ 最新规范）
- 构建工具：Gradle 自动拉取平台 SDK，无需手动配置
- 调试方式：runIde 沙箱 IDE 运行

5.2 开发流程
1. 配置 plugin.xml 注册右键菜单 Action
2. 实现基础弹窗与项目上下文获取
3. 定义 DiagnoseResult 数据模型
4. 逐一对接各环境检测器
5. 统一封装诊断结果与展示逻辑
6. 调试、修复线程报错、菜单显示问题
7. 打包发布插件 zip 安装包

5.3 开发踩坑总结（真实项目排错记录）
- 菜单不显示问题：新版 IDEA 对 group 标签强制要求 text 属性，空文本会直接隐藏菜单
- EDT 线程报错：新版平台禁止在 update() 读取文件与上下文，必须使用 BGT 后台线程更新状态
- 沙箱大量警告：社区版缺少旗舰插件，属于无害警告，可直接忽略
- **中文Windows Kotlin编译编码问题**：UTF-8源文件中 `$variable` 紧跟中文字符时，Kotlin编译器以GBK读取导致多字节序列误解析，报 `Syntax error: Expecting ')'`。解决方案：使用字符串拼接 `"..." + var + "..."` 替代插值
- ToolbarDecorator 无 `setAddButtonText()` 方法，`JBUI.emptyInsets()` 不接受参数且返回类型非 `Border`

六、项目优势与价值
1. 极低入门门槛：避开复杂 PSI 源码解析，纯配置文件读取，新手完全可以独立完成
2. 实用性极强：解决所有 Java 新手最头疼的项目环境爆红问题
3. 扩展性极高：模块化检测器设计，后续可无限新增检测项
4. 轻量化无依赖：仅使用官方 API，插件体积小、运行稳定
5. 文档友好、适合开源：逻辑清晰、场景通用，非常适合作为博客、开源、结课大作业项目

七、版本规划
V1.0 基础版（已完成）
- 右键菜单正常注册、无报错
- 项目环境检测框架搭建完成
- 稳定兼容新版 IDEA 平台

V1.1 功能完善版（已完成）
- 完整实现 JDK / Maven / Gradle / Git / 编码检测
- 弹窗展示完整诊断报告与修复方案
- 支持一键复制诊断报告
- 三色分级表格展示

V1.2 增强版（远期）
- 一键修复、一键生成配置文件
- 报告导出、日志保存
- 拓展 Spring、Docker、MyBatis 环境检测

文档版本：V1.1
编写时间：2026-08-11
